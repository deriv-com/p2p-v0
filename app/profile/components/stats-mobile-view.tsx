"use client"

interface StatsMobileViewProps {
  onBack: () => void
}

export default function StatsMobileView({ onBack: _onBack }: StatsMobileViewProps) {
  return <div>{/* Empty for now */}</div>
}
